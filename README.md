# pizza-delivery

A micro-service based on the Order entity domain with CRUD functionality that simulate orders from a pizzeria.

- In the Startup class, the SeedDatabase method adds 2 customers and 2 requests to the in-memory database;

- To use it, just run the API project and perform CRUD operations via swagger;